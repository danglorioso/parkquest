diff --git a/src/HMRClient.native.ts b/src/HMRClient.native.ts
index 1234567..aaaaaaa 100644
--- a/src/HMRClient.native.ts
+++ b/src/HMRClient.native.ts
@@ -1,3 +1,3 @@
-const HMRClient = require('react-native/Libraries/Utilities/HMRClient');
+const HMRClient = require('react-native/Libraries/Utilities/HMRClient').default;

 export default HMRClient;
diff --git a/src/messageSocket.native.ts b/src/messageSocket.native.ts
index 2345678..bbbbbbb 100644
--- a/src/messageSocket.native.ts
+++ b/src/messageSocket.native.ts
@@ -1,7 +1,7 @@
 /* eslint-env browser */

 function createWebSocketConnection(path: string = '/message') {
-  const getDevServer = require('react-native/Libraries/Core/Devtools/getDevServer');
+  const getDevServer = require('react-native/Libraries/Core/Devtools/getDevServer').default;
   const devServer = getDevServer();
   if (!devServer.bundleLoadedFromServer) {
     throw new Error('Cannot create devtools websocket connections in embedded environments.');
@@ -9,6 +9,6 @@

   const devServerUrl = new URL(devServer.url);
   const serverScheme = devServerUrl.protocol === 'https:' ? 'wss' : 'ws';
-  const WebSocket = require('react-native/Libraries/WebSocket/WebSocket');
+  const WebSocket = require('react-native/Libraries/WebSocket/WebSocket').default;
   return new WebSocket(`${serverScheme}://${devServerUrl.host}${path}`);
 }
